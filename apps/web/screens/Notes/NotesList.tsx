import CollabList from "@pages/Collab/CollabList"

const NotesList = () => (
  <CollabList type="note" route="/notes" iconName="document" title="Notes" createLabel="New note" />
)

export default NotesList
