/**
 * Empty stand-in for the optional `nucleo-core-fill-24` icon pack. When no
 * Nucleo license key is configured the package isn't installed, and the
 * bundler aliases the package specifier to this module so the build still
 * resolves. With no exported icons, every key falls back to Tabler.
 */
export {}
