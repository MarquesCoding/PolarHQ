import SwiftUI

struct PasswordsView: View {
    var body: some View {
        ScreenScaffold(title: "Passwords") {
            ComingSoon(icon: "key", message: "Your encrypted vault will live here.")
        }
    }
}
